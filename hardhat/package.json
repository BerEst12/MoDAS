{
  "name": "modas-token",
  "version": "1.0.0",
  "description": "MoDAS Token - Mode DeFi Agent Swarm Token",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "clean": "npx hardhat clean",
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy.js --network mode_sepolia",
    "verify": "npx hardhat run scripts/verify.js --network mode_sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.17"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0",
    "dotenv": "^16.4.7",
    "node-fetch": "^2.7.0"
  }
}
